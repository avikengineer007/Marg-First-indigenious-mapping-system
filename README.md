# Marg-First-indigenious-mapping-system
Marg is a self-hosted, India-scoped mapping and routing engine — deterministic REST APIs for geocoding, search, tiles, and foot/car/bike routing on OpenStreetMap data. Built standalone with rate limiting, fail-closed validation, and CI-enforced dependency and secrets scanning
